{
  "name": "fmcsa-lookup-backend",
  "version": "1.0.0",
  "description": "Server-side FMCSA QCMobile lookup proxy for the Diesel Service invoicing app",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && npm test"
  },
  "dependencies": {
    "express": "4.22.1"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
